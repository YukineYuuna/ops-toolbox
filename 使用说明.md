# 运维工具箱 v3.1 使用说明

这是三套原生工具目录共用一套离线桌面界面的运维工具箱。启动时自动识别系统，只加载当前平台的工具和权限流程。

## 平台版本

| 平台 | 工具规模 | 专属能力 |
| --- | ---: | --- |
| Windows | 78 个 | SFC / DISM、Windows Update、注册表、引导修复、服务与系统管理入口 |
| macOS | 44 个、9 类 | APFS、Time Machine、FileVault、Gatekeeper、SIP、launchd、Homebrew、Core Audio |
| Linux | 43 个、10 类 | systemd、启动分析、包管理器、SMART、挂载点、SSH 登录、防火墙、PipeWire |

Windows 命令不会出现在 macOS/Linux 中；macOS 的命令也不会被 Linux 调用。

## 启动

- Windows：双击 `运行工具箱.bat`，或运行 `运维工具箱.py`。
- macOS：双击 `运维工具箱_mac.command`。
- Linux：执行 `chmod +x 运维工具箱_linux.sh && ./运维工具箱_linux.sh`。

程序优先使用本地 WebView 界面。WebView 依赖不可用时会自动回退到 Tkinter 界面，核心工具仍可离线使用。

## 界面

- 壁纸由樱道随机图片 API 提供；下载后统一校验并处理为 1920×1080 WebP。
- 最多保留 8 张持久缓存，断网时直接轮播缓存和内置离线图，不会出现空背景。
- 启动联网时每 6 小时至多自动检查一次，每次最多访问接口 2 次。
- 侧栏“更新壁纸”会建立最多 6 个独立请求会话并按内容去重，获取到新图后立即缓存和切换。
- 桌面侧栏和移动端汉堡菜单。
- 日间/夜间模式。
- 字体大小 85% 至 140%，设置保存在用户配置目录。
- 收藏、最近使用、分类搜索、工具详情、危险操作确认和运行日志。

## AI 助手

默认模式是离线诊断：根据当前系统的工具名称、说明和故障关键词推荐候选工具，不需要模型、不需要网络。

可选增强：

- Ollama：本机运行，默认连接 `http://127.0.0.1:11434/api/chat`。
- OpenAI 兼容接口：可填写 OpenRouter 等服务的 HTTPS 地址、模型和用户自己的 API Key。

没有内置公共 API Key。在线接口或 Ollama失败时自动回落到离线诊断。AI 只给建议，运行工具仍需用户点击；危险工具仍需二次确认。

如不希望把 Key 写入磁盘，不勾选“记住 Key”，也可以设置环境变量 `TOOLBOX_AI_API_KEY`。

## 打包

原生程序必须在目标系统上构建，PyInstaller 不能在 Windows 上交叉生成 macOS `.app` 或 Linux 可执行文件。

- Windows：运行 `build_exe.bat`，输出 `dist\Windows运维工具箱_v3.exe`。
- macOS：运行 `chmod +x build_macos.sh && ./build_macos.sh`，输出 `dist/macOS运维工具箱.app`。
- Linux：运行 `chmod +x build_linux.sh && ./build_linux.sh`，输出 `dist/Linux运维工具箱`。

Linux WebView 需要系统 WebKitGTK。Debian/Ubuntu 可安装 `python3-gi gir1.2-webkit2-4.1`。macOS 正式分发前应执行代码签名与 notarization。

## 离线边界

完全离线可用：界面、已缓存壁纸、搜索、收藏、系统监控、平台工具、离线诊断和日志。

需要网络：获取新壁纸、检查系统/软件包更新、用户主动选择的在线 AI 接口，以及部分本来就要访问系统更新源的运维命令。

壁纸和第三方组件的授权说明见 `web/assets/ATTRIBUTION.md` 与 `web/vendor/LUCIDE_LICENSE.txt`。
